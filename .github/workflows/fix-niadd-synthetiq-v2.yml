name: Obsolete Niadd pagination fix

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Niadd was replaced by NoxManga in Synthetiq; this workflow is intentionally disabled."
